<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label :keywords, "Keywords: accepts a comma separated list" %><br>
    <%= f.text_field :keywords %>
  </div><br>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div><br>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, size: "100x10" %>
  </div><br>

  <div class="actions">
    <%= f.submit "Save", class: "btn btn-default" %>
  </div>
<% end %>
